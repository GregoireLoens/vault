//! `vault` — interface en ligne de commande.
//!
//! Les commandes arrivent avec la phase 3 (T045 et suivantes).

fn main() {
    println!("vault — non implémenté (phase 1 : environnement uniquement)");
}
